/** Заголовок колонки панели голосования на beta.jugru.org. */
export interface VotingPanelColumn {
  /** Порядковый индекс в `thead` (0-based). */
  index: number;
  /** Отображаемое имя колонки. */
  name: string;
}
